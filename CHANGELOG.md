# Changelog

## [v1.9.0] - 2026-07-19

### 개선 (지도 정밀도)

- **전국 해안선 정밀도 대폭 향상**: 일본 시정촌·도도부현, 한국까지 지도 경계를
  더 상세한 데이터로 교체. 도쿄만(오다이바·도요스·하네다)·오사카만(간사이공항·
  유메시마)·이세만(나고야항) 등 매립지와 나가사키 다도해·오키나와 열도 같은
  섬들이 실제 지형대로 표시됩니다. 광역 지도도 시정촌과 같은 경계로 맞춰 두
  레이어가 어긋나지 않습니다.
- **지역 공유 카드에 부속도서 인셋**: 시정촌 지역 카드에서 멀리 떨어진 섬(오키나와
  미야코·이시가키, 도쿄 이즈제도, 시마네 오키섬, 가고시마 아마미 등)이 잘리지
  않고 우하단 인셋 박스에 모여 표시됩니다.

### 변경

- **공유 창 기본 선택**: '지도 공유하기'가 지금 보고 있는 국가를 기본으로 선택
  (기존: 양국 기록이 있으면 양국)

### 기타

- **라이선스**: 코드에 MIT 라이선스 추가 (지도 데이터는 각 원출처 라이선스를 따름)

---

## [v1.8.0] - 2026-07-17

### 추가 (지역 완주 도장)

- **광역 완주 도장**: 한 도도부현/시도의 소속 시정촌·시군구를 전부 방문하면
  그 광역의 도장을 발급 (도쿄 전역 → `東`, 오사카 → `大阪`). 완주한 광역만
  도장첩에 노출. 첫 글자가 겹치는 광역은 2자로 새겨 구분 (大阪/大分, 山形/山口,
  忠北/忠南, 慶北/慶南 등)
- **권역 완주 도장**: 시코쿠(`四国`)·전라(`全`) 권역 도장 추가
  (기존 간사이·간토·규슈·수도권·충청·경상과 동일 방식)
- **지역 완주 도장은 藍(쪽빛) 파랑**으로 표시해 마일스톤 도장(인주 레드)과 구분

### 변경 (GPS · 레벨)

- **GPS = "현재 위치 도장" 원샷**: 버튼을 누르면 현재 위치를 1회 측정해 어느
  나라인지 자동 판정 후 그 지역에 도장. 웹앱에서 불가능한 실시간 추적 대신
  내 위치를 도장처럼 찍는 방식으로 단순화 (연속 추적 기능은 내부 보존)
- **여행자 레벨 국가별 표시**: 레벨이 지도의 일본/한국 토글을 따라 각 나라별로
  표시 (양국 뷰에서는 합산)

### 기타

- **공유 링크 딥링크** `?c=jp` / `?c=kr` 로 초기 지도 국가 지정 (유입 추적
  파라미터는 보존)

---

## [v1.7.2] - 2026-07-17

### 개선 (지역 카드 공유 프리뷰)

- **지역 카드에 공유 프리뷰 창**: 시정촌/시군구 모달의 캡처 버튼을 명확한
  '공유하기' 버튼으로 바꾸고, 누르면 카드 프리뷰가 떠서 지명 표시를
  켜고 끄며 바로 확인 후 공유/저장 (기존: 토글해도 화면 변화가 없어
  안 먹히는 것처럼 보였음 - 프리뷰 = 저장본)
- **기본값 조정**: 공유 카드 지명 표시 켬 · 도장첩 표시 끔,
  지역 카드 지명 표시 켬

---

## [v1.7.1] - 2026-07-17

### 개선 (카드 3종 UI 통일)

- **오키나와 인셋 통일**: 광역 카드도 기초 카드와 같은 좌상단 사각 인셋으로
  (기존: 우하단 대각선 컷). 박스 비율을 열도 범위에 맞춰 섬이 약 40% 크게
- **2배 해상도 렌더**: 모든 카드·지도 스냅샷을 2x로 그려 좁은 기초 지역도
  확대하면 선명하게
- **지역 카드 형식 통일**: 헤더(経 인장+MAPEXP+부제)·방문 지역/달성률/EXP
  3열 통계·푸터를 국가 카드와 동일한 형식으로
- **지명 라벨 옵션**: 공유 카드 '지명 표시' 토글 - 광역 지도는 광역명,
  기초 지도도 광역명만(전국 축척에서 기초명은 판독 불가), 지역 카드는
  기초명. 넓은 지역 우선 배치하고 겹치는 라벨은 자동 생략
- **도장첩 표시 옵션**: 공유 카드에서 달성 도장 노출 켜고 끄기,
  8개 초과 시 7개 + '+N' 표기

---

## [v1.7.0] - 2026-07-17

### 변경 (공유 카드 전면 개편)

- **카드를 캔버스로 직접 렌더**: DOM 캡처(html2canvas)를 완전히 제거하고
  지역 카드처럼 국가 카드도 캔버스에 직접 그림. CJK 가변 폰트 메트릭
  오측정으로 글자가 아래에 붙거나 일본어 자간이 벌어지던 문제가 근본 해결
- **점수를 Lv/EXP 게임 형식으로**: `Lv.3` 대형 타이포 + `26 EXP` +
  다음 레벨까지 진행 바 + 티어 칭호
- **방문 분포를 스택 막대로 복귀**: 등급별 색이 한 막대에 쌓이고
  아래 중앙에 등급명·개수 범례
- **미리보기 = 저장본**: 모달 미리보기가 실제 저장/공유되는 카드 이미지
  그대로 (WYSIWYG)
- html2canvas 의존성 제거 (번들 경량화)

---

## [v1.6.9] - 2026-07-17

### 수정 (공유 카드)

- **기존 구성으로 복귀 + 개선 유지**: 지도를 다시 카드 상단에 (큰 지도·
  대각선 오키나와 유지), 점수는 지도 아래 대형 타이포로 유지
- **숫자 주변 간격 벌어짐 해결**: '도장 1 점', '17 %'처럼 캡처에서 숫자
  간격이 깨지던 문제 - 카드에 한해 시스템 폰트 + 기본 숫자 메트릭 고정
  (본문의 tabular-nums·가변 폰트를 캡처 라이브러리가 잘못 측정)

---

## [v1.6.8] - 2026-07-17

### 개선 (공유 카드 시인성)

- **점수를 카드의 주인공으로**: '도장 N점'을 좌측 대형 타이포로, 아래에
  여행자 레벨·티어 병기
- **등급 사다리 범례**: 색 세그먼트 + 등급명 + 개수를 지도 아래 한 줄에
  (웰컴 모달과 같은 모티프 - 스택 바 대체)
- **오키나와 인셋을 우하단 대각선 컷으로**: 일본 지도 관례를 따르고
  본토를 더 크게 (기존: 좌상단 박스)

---

## [v1.6.7] - 2026-07-16

### 개선

- **세부 도장 → 광역 동기화 강화**: 시정촌/시군구 도장을 찍으면 부모 광역이
  '자식들 중 최고 레벨'로 상향 동기화. 기존에는 방금 바꾼 자식만 비교해,
  광역을 직접 내린 뒤 다른 세부를 찍으면 더 높은 기존 세부 기록이 무시됐음
  (내림 동기화는 광역 직접 기록 보호를 위해 하지 않음)

---

## [v1.6.6] - 2026-07-16

### 기능

- **지역별 이미지 캡처**: 세부 지역 도장 화면에서 그 광역의 시정촌/시군구
  색칠 지도를 카드 이미지로 저장·공유 (지역명·점수·진행 포함, 먼 섬은
  본체 위주로 컷). 전부 캔버스로 그려 배치 깨짐 없음

### 수정

- 파일 공유 시트를 터치 기기에서만 시도 - 일부 데스크톱 환경에서
  공유 API가 멈춰 아무 일도 일어나지 않던 문제 (데스크톱은 바로 다운로드)

---

## [v1.6.5] - 2026-07-16

### 개선

- **공유 카드 일본 지도 대형화**: 오키나와·오가사와라 낙도가 지도를 절반
  크기로 축소시키던 것을 본토 고정 범위로 확대. 오키나와는 좌상단 인셋
  박스에 따로 표시 (광역·기초 지도 모두), 오가사와라는 크롭
- **공유 카드 배치 어긋남 수정**: 티어 필·범례 칩·도장 한자의 세로 정렬이
  캡처 시 깨지던 문제 (baseline → 명시적 정렬로 교체)
- **편집 잠금**: 지도 패널에 잠금 토글 - 이동/구경 중 실수 탭으로 등급이
  바뀌는 것을 방지 (설정은 기기에 기억, 상세 보기는 계속 가능)

---

## [v1.6.4] - 2026-07-16

### 개선

- **세부 지도가 보고 있던 곳으로 열림**: 지도에서 세부 지역 도장에 들어가면
  현재 화면 중심의 광역으로 바로 열림 (규슈를 보고 있었으면 가고시마부터)
- **지도 컨트롤 패널 접기 - 데스크톱에도**: 항상 떠 있던 패널을 토글 버튼으로
  접었다 펼 수 있음 (데스크톱 기본 열림, 모바일 기본 닫힘)

---

## [v1.6.3] - 2026-07-16

### 개선

- **현 위치 도장 시 세부 지도가 그 지역으로 줌 이동**: 광역 점프에 더해
  미니맵이 감지된 시정촌/시군구 주변으로 확대 (수동으로 광역을 바꾸면
  다시 전체 보기)
- **지도 위 세부 지역 진입 버튼을 텍스트로**: 아이콘만으로는 기능을 알 수
  없어 '시정촌/시군구 도장' 텍스트 알약 버튼으로 교체

---

## [v1.6.2] - 2026-07-16

### 수정

- **모든 토스트가 자동으로 사라지도록 전역 수정**: 도장 획득(뱃지)·GPS 등
  토스트가 다른 행동 없이는 안 사라지던 문제 - v1.5.3의 자체 소멸 타이머를
  전역 래퍼(appToast)로 확장해 모든 토스트에 적용

### 기능

- **세부 지역 현 위치 도장**: 시정촌/시군구 도장 화면에 GPS 버튼 추가 -
  누르면 지금 있는 광역으로 화면이 점프하고 해당 세부 지역에
  접지(2) GPS 인증 도장이 찍힘

---

## [v1.6.1] - 2026-07-16

### 개선

- **네비게이션 재배치 (모바일 밀도 완화)**: 국가 전환(JP/KR) 토글을 지도
  우상단으로 이동 - 지도의 대상과 붙어 있어 인지 쉬움. 모바일 공유 버튼은
  아이콘만, 헤더 간격 정리
- **데이터 파일 견고화**: 내보내기에 스키마 버전+앱 식별자 봉투
  (엉뚱한 JSON 즉시 거부, 향후 구조 변경 대비). 가져오기는 전체 필드 타입
  검증 후에만 반영 - 깨진 파일로 앱이 백지가 되는 경로 차단
- **가져오기 안전장치**: "기존 N개 지역이 파일의 M개로 교체됩니다" 구체적
  확인 + 진행 시 현재 데이터 자동 백업 다운로드
- **파일 공유 동선**: 모바일은 Web Share로 메신저에 바로 전송(미지원 시
  다운로드 폴백), 파일명에 이름(선택) 포함 - mapexp_이름_날짜.json
- SEO: robots.txt·sitemap.xml 추가, README 공유 기능 설명 갱신

---

## [v1.6.0] - 2026-07-16

공유 확장(비교·QR·파일 교환·티어), 첫 방문 개선(지도 선택·経 파비콘),
일본어 용어 정비(旅スタンプ + 経県値 지시적 표기).

### 변경 (상표 존중)

- **일본어 UI 용어를 経県値 → 旅スタンプ 계열로 교체**: 経県値는 uub.jp의
  등록상표라 서비스명·기능명으로의 사용을 중단 (타이틀·부제·통계·메뉴 등
  13곳). 푸터의 개념 원조 크레딧(지시적 사용)은 유지
- OG 이미지 alt·README 일본어판 제목 동일 적용, 스크린샷 15장 재캡처
- **지시적 표기는 적극 활용**: 웰컴 모달 등급 사다리에 '経県値®(uub.jp)와
  같은 6단계 기준' 표기, 메타 설명·키워드에 経県値(경현치) 참조 언급,
  README 일본어판 소개문 강화 - 제품명이 아닌 출처 귀속 형태로만

### 기능 (공유 확장)

- **내 지도와 비교(겹쳐보기)**: 공유 지도를 열람할 때 '내 지도와 비교'를
  켜면 나만/상대만/둘 다 3색으로 두 기록을 한 지도에 대비 (범례 자동 전환)
- **QR 코드 공유**: 공유 모달에서 QR 생성 - 카메라로 찍으면 지도가 열림
  (오프라인·대면 공유용)
- **파일로 교환**: 공유 모달에서 JSON 내보내기 + 받은 파일 열기 -
  받은 파일은 내 기록을 건드리지 않고 보기 모드로 열림
- **공유 카드에 순위 티어**: 달성률 구간별 칭호(여행 새싹 → 동네 탐방가 →
  지도 순례자 → 정복자 → 제패왕)를 카드에 표시

### 개선 (UI 마감)

- **도장첩 뷰 반응형 기본값**: 데스크톱은 도장 그리드(사이드바가 길어지지
  않게), 모바일은 리스트. 데스크톱에서 리스트 선택 시 내부 스크롤로 높이 고정
- **데스크톱 사이드바 = 지도 높이 고정**: 두 컬럼 아랫단이 항상 맞고,
  넘치는 통계는 사이드바 내부 스크롤로
- **파비콘·PWA 아이콘을 経 인장으로**: 헤더 워드마크와 동일한 디자인
  (기존: 보라색 핀 기본 아이콘). SVG 파비콘 + 192/512/maskable/apple-touch 재생성
- **웰컴 모달에 시작 지도 선택 추가**: 언어(한국어·日本語·English 순) 선택 후
  지도(한국/일본)도 직접 선택 - 언어가 기본값을 정해주고 바꿀 수 있음.
  언어 순서는 설정·헤더 드롭다운에도 동일 적용

- **도장 토스트 알약형 마감**: 레이어드 섀도 + 곡률에 맞춘 버튼 배치,
  '세부 도장'이 주 버튼(인주 칩)·실행취소는 보조로 위계 정리
- **모달·폼 디자인 토큰 통일**: 스캐폴딩 시절의 회색/파랑 기본색
  (gray-*, ring-blue, red-500)을 페이퍼&인주 토큰으로 전면 교체,
  모달 라운드·섀도·닫기 버튼 마감 개선
- 라디오·체크박스·날짜 입력의 강조색을 인주 레드로 (브라우저 기본 파랑 제거)
- 전역 토스트 마감 통일 (12px 라운드, 레이어드 섀도, 패딩)

### 수정

- 언어 전환 시 헤더 버튼(공유·언어 표시) 폭이 조금씩 변해 레이아웃이
  흔들리던 문제 - 최소 폭 고정으로 3개 언어 모두 동일 폭

---

## [v1.5.3] - 2026-07-16

### 수정

- **도장 토스트가 사라지지 않고 쌓이던 문제**: 터치 기기에서 토스트를 탭하면
  라이브러리 내부 소멸 타이머가 일시정지된 채 풀리지 않는 경우가 있어,
  소멸을 자체 타이머로 강제. 토스트 아무 곳이나 탭해도 즉시 닫힘

---

## [v1.5.2] - 2026-07-16

### 개선

- **도장 토스트에 '세부 도장' 버튼**: 광역을 찍으면 그 지역의 시정촌/시군구
  도장 지도로 바로 이어지는 버튼이 하단 토스트에 함께 표시
- **세부 지도 배경 켬/끔 토글**: 기억 단서(도로·역)가 필요할 때는 배경 지도,
  깔끔한 색 지도가 필요할 때는 끔 - 선택은 기기에 기억됨

---

## [v1.5.1] - 2026-07-16

### 개선 (모바일 편집 흐름)

- **지도 위 시정촌/시군구 도장 버튼**: GPS 버튼 위 인주색 원형 버튼으로
  지도 화면에서 바로 진입 (모바일·데스크톱 공통)
- **도장 피드백을 하단으로**: 등급 변경 토스트가 지도 중앙 대신 하단에서
  표시되고, 연속으로 탭해도 하나만 갱신 - 지도를 가리지 않고 연속 편집
- **터치 기기에서 탭 툴팁 제거**: 탭한 지역 위에 뜨던 툴팁이 지도를 가리던
  문제 (데스크톱 호버 툴팁은 유지)

---

## [v1.5.0] - 2026-07-16

첫 방문 경험(웰컴 모달)과 세부 지역 도장 발견성 강화, 도장첩 20종 확장.

### 개선

- **도장첩 확장 12 → 17종(뷰 기준)**: 한국 권역 뱃지(首 수도권·忠 충청·慶 경상
  — 지역 뱃지는 보고 있는 국가 것만 표시), 기초 지역 뱃지(巷 10곳·洞 50곳·
  坊 100곳), GPS 대장정(長 100km), 양국 뱃지(渡 바다를 건너)

- **시정촌/시군구 도장 = 지도 우선**: 모달이 지도 뷰로 열림 (목록은 검색·일괄용 보조)
- **미니맵 실제 지도 배경**: 도로·지명·수계가 보여 방문 여부를 떠올리기 쉬움
- **미니맵 등급 색 범례**: 지도 하단에 색 가이드 (호버 시 한 줄 설명)
- **미니맵 화면 맞춤 개선**: 본토 폴리곤 실제 경계 합집합으로 타이트하게
  (먼 섬 이상치는 중앙값 기반 컷)
- **UI 언어 변경 시 지도 지명도 함께 전환**: 언어를 바꾸면 지명 언어가
  '자동'으로 리셋. 지도 패널에서 지명 언어를 직접 고르면 지도만 따로 유지
- 헤더 부제·OG 문구 간결화 ('여행 도장 지도')
- **첫 방문 웰컴 모달**: 언어 선택(즉시 반영·기본 국가 연동) + 5줄 매뉴얼
  (탭/길게 누르기/세부 지역/GPS/프라이버시) + 등급 색 사다리
- **세부 지역 도장 진입점 확대** (핵심 기능 발견성): 지도 컨트롤 패널
  상단 버튼, 지역 상세 모달 버튼(해당 광역으로 바로 열림), 온보딩 매뉴얼 안내
- **도장첩 리스트 뷰**: 업적을 이름·조건·진행률 바와 함께 리스트로
  (기본), 기존 도장 그리드는 토글로 유지

---

## [v1.4.0] - 2026-07-16

経県値 원조 표준 팔레트 적용과 UX 전면 개선 — 실행취소·롱프레스 상세·
첫 방문 언어 감지·지명 라벨 현지화.

### 개선

- **経県値 원조 표준 팔레트 적용**: 지도·범례·카드·공유 이미지 색상을
  uub.jp 원조 색(거주 적자/숙박 빨강/방문 노랑/접지 연두/통과 하늘)으로 통일
- **여행자 레벨 양국 합산 고정**: 국가 탭을 전환해도 레벨이 출렁이지 않음
  (레벨 카드에 '일본 × 한국' 표기)
- **등급 변경 실행취소**: 지도·리스트에서 탭으로 등급을 바꾸면
  토스트에서 한 번에 직전 등급으로 되돌리기 가능
- **상세 진입 개선**: 지도 우클릭/길게 누르기 = 상세 모달(모바일 지원),
  리스트 카드에 연필 버튼 추가 (기존 Shift+클릭도 유지)
- **공유 지도 범례 상시 표시**: 공유 링크로 열람하는 사람이
  색의 의미를 바로 알 수 있게 뷰어 모드에서 범례 노출
- **GPS 배너 시정촌 이름 다국어 표기** (기존: 한자 원어 고정)

### 수정

- 국가 전환만 해도 이미 달성한 뱃지가 새로 획득된 것처럼 토스트되던 버그
- 시정촌/시군구 관리 모달이 ESC로 닫히지 않던 문제
- 한국어 버튼 문구가 단어 중간에서 줄바꿈되던 문제 (word-break: keep-all)

### 개선 2차

- **미답 지역 톤 다운**: 배경 지도 위 미답 지역에 종이톤 반투명을 덮어
  도로·지명이 방문 지역보다 시끄럽던 시각 반전 해소
- **첫 방문 언어 감지**: 브라우저 언어로 UI 언어·기본 국가 자동 설정
  (한국어 → 한국 지도, 日本語 → 일본 지도)
- **온보딩 간소화**: 핵심 한 줄(탭=도장)로 축소, 카드 밖 탭으로도 닫힘 —
  첫 탭이 곧 첫 도장이 되는 흐름
- **등급 용어 설명**: 지도 툴팁에 등급 한 줄 설명 추가
  ('접지 — 잠깐 내렸다' 등), 범례·접지 기록 버튼에 호버 설명
- **국가 전환 UI 통일**: 헤더 JP/KR 토글 하나로 (본문 중복 선택기 제거)
- **공유 모달 CTA 위계**: 모바일은 '공유하기'가 주 버튼, 링크 복사는 보조
- **리스트 그룹명 3언어**: 지방/권역 이름(도호쿠·수도권 등)이
  영어·일본어 UI에서도 번역 표시
- **지역별 점수 스코프 정합**: 단일 국가 뷰에서는 현재 국가 지역만 표시
- 리스트 카드 키보드 접근성 (Tab 포커스 + Enter/Space 조작)
- '다음 레벨까지 n' → 'n점' 단위 표기
- 지도 줌 버튼을 디자인 시스템에 맞게 스타일링, 모바일은 숨김(핀치 줌)
- 통계 분포 바 대비·두께 개선 (트랙이 안 보이던 문제)
- '{기초 지역} 관리' → '{기초 지역} 도장 찍기' (일본어는 経県値 용어)

### 개선 3차 (지명 라벨)

- **지명 라벨 기본값을 '직접 표시'로**: 타일 원본(CARTO) 라벨은 로마자
  위주라 '지명 언어' 설정이 안 먹었음 — 자체 라벨은 설정(자동=UI 언어)을
  항상 따름. '지도 원본'은 옵션으로 유지
- **양국 지도에서 반대 국가 라벨 표시** (기존: 현재 국가만 이름이 보임)
- **일본어 지도 라벨 간결화**: 한국 지명의 가나 병기 괄호를 지도 라벨에서
  제거 ('江原特別自治道(カンウォン)' → '江原特別自治道'). 툴팁은 가나 유지

### 문서

- README 3언어 갱신 (원조 팔레트·실행취소·롱프레스·합산 레벨 반영)
  + 스크린샷 15장 재캡처

---

## [v1.3.0] - 2026-07-10

Vercel 정식 배포(https://mapexp.vercel.app) 및 양국·시정촌 시스템 완성.

### 기능

- **양국 동시 뷰 통계**: 양국 지도가 켜져 있으면 일본+한국 합산 레벨과
  국가별 방문/달성률/분포를 함께 표시
- **전국 시정촌 뷰**: '기초 지역' 토글을 켜면 줌과 무관하게 전국이
  시정촌/시군구 단위로 표시(캔버스 렌더). 이 상태에서는 지도 수정 불가 —
  구경 모드와 기록 모드 분리 (기본값: 끔)
- **시정촌 관리 지도 뷰**: 관리 모달에 목록/지도 토글 — 미니맵에서
  지역을 직접 눌러 레벨 순환, 지역 이름 라벨 표시
- **지명 3개 언어**: 일본 시정촌 1,918개 한글·로마자 표기(총무성 읽기 기반
  자동 생성), 한국 시군구 250개 한자+가타카나 표기. 광역·기초 언어 통일
- **지명 언어 옵션**: 지도 위 지명을 UI 언어와 별도로 선택
  (자동/한국어/日本語/English 순환)
- **공유 카드 선택기**: 국가(일본/한국/양국) × 상세도(광역/기초/둘다) 조합,
  국가별·기초 점수 캡션, 모바일 공유 시 카드 PNG 파일 첨부
  (X·인스타에 이미지로 공유, 미지원 시 링크 폴백)
- **지역별 점수 통계**: 방문 광역별 등급 + 산하 기초 점수·개수,
  전체 기초 점수 합계 (국가 무관 전체 기록 표시)

### 주요 수정

- 지도 탭 직후 툴팁이 이전 등급을 보여주던 오프바이원 수정
- 지역·국가 전환 시 라벨만 바뀌고 폴리곤은 이전 것이 남던
  스테일 렌더 수정 (미니맵·전체맵·국가 전환 모두)
- 사이드바가 길어지면 데스크톱 지도가 화면 밖까지 늘어나던 레이아웃 수정
- Next.js 16.0.1 → 16.2.10 (보안 취약 버전 교체)
- 所属未定地(무주지) 데이터 제외

### 문서

- README 3언어 개편(라이브 URL, 양국 포지셔닝) + 스크린샷 15장 갱신

---

## [v1.2.0] - 2026-07-07

첫 공개 릴리스.

### 기능

- **경현치 기록**: 일본 47 도도부현 + 1,897 시정촌, 한국 16 시도 + 250 시군구.
  클릭 순환(0→5→0), 상세 모달, 시·군·구 일괄 관리(광역 이동·검색)
- **GPS**: 광역+기초 동시 감지, 빠른 접지 기록, 이동 경로 트랙(Wake Lock),
  자동 방문 감지. GPS 인증 기록은 수정·삭제 불가(수동 기록은 과거 날짜 자유)
- **게임화**: 여행자 레벨, 한자 낙관 도장첩(12종), 레벨업 도장 연출
- **공유**: 읽기 전용 공유 링크(받는 사람 데이터 자동 백업·복원),
  색칠 지도가 담긴 SNS 이미지 카드, 모바일 네이티브 공유
- **다국어**: 한국어 / English / 日本語 (지역명 포함)
- **PWA**: 홈 화면 설치, 지도 데이터 오프라인 캐싱
- **디자인**: "페이퍼 & 인주" 시스템 (와시 뉴트럴 + 인장 레드, Pretendard)

### 데이터

- 2026-07 행정구역 반영: 전남광주통합특별시 출범, 군위군 대구 편입
  (구버전 기록 자동 마이그레이션)
- 일본 도도부현 GeoJSON 13.4MB → 181KB 간소화

### 주요 수정

- 지도 레이어 재마운트로 인한 연속 클릭 유실 및 줌 복귀 후 클릭 불능 해결
- 폴리곤 감김 방향(winding order)으로 인한 지역 감지·렌더링 오류 해결
- 공유 링크가 받는 사람의 지도를 덮어쓰던 문제 → 뷰어 모드로 재설계

---

## [v1.1.0] - 2026-01-20

- 경현도 6단계 레벨 체계(0~5), 지도 표시 모드(Standard/Simple),
  도쿄 시정촌 지원, URL 공유, 통계 패널 등 MVP 기능
